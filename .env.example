# === DJANGO CORE ===
DEBUG=True
SECRET_KEY=django-insecure-CHANGE-THIS-IN-PRODUCTION-12345678901234567890
ALLOWED_HOSTS=localhost,127.0.0.1

# === DATABASE ===
DB_ENGINE=django.db.backends.postgresql
DB_NAME=quizzy
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_HOST=localhost
DB_PORT=5432

# === REDIS & CACHE ===
REDIS_URL=redis://localhost:6379/0

# === CELERY ===
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/1

# === EMAIL ===
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-specific-password
DEFAULT_FROM_EMAIL=noreply@quizzy.com
SERVER_EMAIL=noreply@quizzy.com

# === SECURITY ===
SECURE_SSL_REDIRECT=False
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False

# === CORS ===
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000

# === FEATURE FLAGS ===
DAILY_QUIZZES_PER_USER=2
KEYS_PER_AD=1
DAILY_AD_LIMIT=20
KEYS_REQUIRED_PER_UNLOCK=10
AD_WATCH_MIN_SECONDS=30
KEY_RESET_HOUR=0
REPORT_GENERATION_HOUR=20
LEADERBOARD_CACHE_TTL=300
USER_STATS_CACHE_TTL=600

# === SENTRY (Optional) ===
SENTRY_DSN=

# === AI CHATBOT (Prof. Curio) ===
OPENAI_API_KEY=


#for render ->
# Commit & Push my latest changes to Render.
# Go to Render Dashboard > Environment.
# Update these variables:
# EMAIL_PORT: 465 (Change from 587)
# Add EMAIL_USE_SSL: True
# Add EMAIL_USE_TLS: False (Important: Disable TLS when using SSL)
